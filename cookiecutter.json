{
  "templates": {
    "gitops": {
      "path": "./gitops",
      "title": "GitOps configs (AppSets) repo",
      "description": "Argo CD ApplicationSets + per-cluster Helm values - the repo_appsets repo"
    },
    "charts": {
      "path": "./charts",
      "title": "Helm charts repo",
      "description": "Private application Helm charts consumed by the GitOps AppSets - the repo_charts repo"
    }
  }
}
